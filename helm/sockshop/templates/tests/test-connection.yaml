apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sockshop.fullname" . }}-test-connection"
  labels:
    {{- include "sockshop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sockshop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
