apiVersion: v1
kind: Service
metadata:
  name: {{ include "sockshop.fullname" . }}
  labels:
    {{- include "sockshop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sockshop.selectorLabels" . | nindent 4 }}
